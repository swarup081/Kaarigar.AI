{
  "name": "kaarigar",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Driven Market Linkage & Smart Cataloging for Artisans — SIH 2026 PS 26090",
  "scripts": {
    "dev:mobile": "turbo run dev --filter=@kaarigar/mobile",
    "dev:backend": "turbo run dev --filter=@kaarigar/backend",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
